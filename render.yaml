services:
  - type: web
    name: growfin-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 4 --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT